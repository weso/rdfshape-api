package es.weso.rdfshape.server.utils

object FUtils {

  /* def cnvResource[A, F[_]: Effect](r: Resource[IO,A]): Resource[F,A] =
   * r.mapK(cnv) */

  /* def cnv[A,F[_]: Effect]: FunctionK[IO,F] = new FunctionK[IO,F] { def
   * apply[A](ioa: IO[A]): F[A] = io2f(ioa) } */

}
